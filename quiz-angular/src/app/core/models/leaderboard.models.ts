export interface TopQuizViewModel {
  rank: number;
  quizTitle: string;
  attemptCount: number;
}

export interface TopUserViewModel {
  rank: number;
  userName: string;
  bestScorePercent: number;
  quizTitle: string;
}
